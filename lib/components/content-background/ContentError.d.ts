import { FC } from "react";
type PropsType = {
    className?: string;
    title?: string;
    titleLink?: string;
    toPage?: string;
    srcImage?: string;
    duration?: number;
    rollBack?: boolean;
};
declare const ContentError: FC<PropsType>;
export default ContentError;
